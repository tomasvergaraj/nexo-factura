# Si es "true", la app funciona con datos de demostración (mock) sin backend.
# Default: false (consume la API real).
VITE_USE_MOCK=false

# Base URL de la API. Default: /api (proxy de Vite hacia el backend).
# VITE_API_BASE_URL=/api
